apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: {{ include "aiops-platform.namespace" . }}
data:
  {{- range $key, $value := .Values.backend.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

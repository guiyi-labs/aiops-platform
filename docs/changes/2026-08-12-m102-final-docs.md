# m102-final-docs：M102 最终交付文档（测试矩阵/已知限制/兼容范围/运维手册/安全声明）

- Date: 2026-08-12
- Status: Complete
- Scope: M102「生成最终测试矩阵、已知限制、兼容范围、运维手册和安全声明」的本地交付——五份文档汇编当前全部可验证证据，并明确授权轨（M89/M90、真实组织演练）未关闭前的 RC 边界。

## Context

M102 范围要求「生成最终测试矩阵、已知限制、兼容范围、运维手册和安全声明」。此前 M102 本地轨道已积累：双全新环境安装/升级/回滚/备份恢复（10/14/16 PASS）、可复现闭环演示（15/15）、WAL/PITR 本地数据轨（8 场景）、OIDC 本地身份轨（14 场景）与 M97 RC 供应链证据；但缺少把这些证据汇编成对外可读的最终交付文档。本改动补齐五件套，并在每份文档显式声明：M89/M90 与真实组织 kind/Helm 演练未完成前版本保持 RC，不宣称 GA。

## What Changed

### 新增文档
- `docs/testing/test-matrix.md`：分层测试矩阵（Go 74 包 / 205 测试文件、覆盖率 60.03% 门禁、OpenAPI↔路由双向契约、vitest 25、Playwright 双视口 26 用例 + M93-C 42/42 回归基线、axe、静态敏感扫描、kind e2e、release 供应链），本地确定性演练汇总表（WAL/PITR 8、dual-env 10/14/16、demo-drill 15、OIDC 14 及报告路径），关键用户旅程的 API + 浏览器双通道证据，以及「未覆盖/待组织环境项」清单（不视为通过）。
- `docs/testing/known-limitations.md`：授权轨限制（M89 OIDC/MFA、M90 WAL/PITR/HA、真实 kind/Helm 生命周期）、演练工具边界（`demo-kube-mock` 最小 API 面、`oidc-provider` 仅演练用、本地离线镜像不入库）、本地环境约束（colima 2GiB OOM、无 kind/helm/pwsh、Docker Hub 不可达、macOS SSL_CERT_FILE）、功能/契约限制（AI 仅解释、固定操作目录、实时查询不落库、性能报告模式）与文档证据缺口。
- `docs/testing/compatibility.md`：工具链兼容（Go 1.26、Node 22/pnpm 11.7、client-go 0.36、PG17+pgvector 0.8.1、Vue 3）、部署形态（compose/Kustomize/Helm/kind/离线包/双架构镜像）、配置面（compose.yaml 全部 env 分组）、端口约定（开发栈与各演练隔离端口）、数据恢复兼容与明确不兼容项（强制 HTTPS kubeconfig、仅 PostgreSQL、离线无 Docker Hub）。
- `docs/operations/runbook.md`：快速开始、`/health/live` + `/health/ready`、升级回滚（`--force-recreate` + version/标记复验）、备份恢复（pg_dump 全新空库还原 + WAL/PITR 演练）、关键旅程验收（登录→态势→根因→证据→受控动作→事故复盘）、日志/审计/供应链门禁命令、故障排查表与演练清理不变量。
- `docs/security/security-statement.md`：身份与访问（四角色、2D 授权矩阵、404 防泄漏、OIDC/MFA fail-closed）、数据保护（凭据加密 + 版本化密钥环、敏感扫描 + 脱敏契约、签名审计归档、逻辑备份 + WAL/PITR）、受控运维边界（固定操作目录、dry-run → 确认 → 幂等 → 审计、目标漂移拒绝）、供应链与发布门禁（govulncheck/pnpm audit/license/镜像基础层/SBOM、cosign fail-closed）、前端安全与 GA 前已知例外表。

### 更新
- `docs/README.md`：文档索引补充 `test-matrix.md` / `known-limitations.md` / `compatibility.md` / `operations/runbook.md` / `security/security-statement.md`。
- `CHANGELOG.md`：Unreleased 新增本里程碑条目。
- `docs/next-long-term-plan.md` / `docs/PROJECT_STATUS.md`：M102 进度与状态基线更新（见下）。

## Verification

- `git status --porcelain` 仅含本改动文件；`scripts/scan-sensitive-fields.sh`：clean。
- 文档内引用均以仓库内既有证据为准（报告文件、脚本名、里程碑 tag、包/测试计数），无新增未经验证的数字；关键数字已与 `docs/testing/README.md`、`docs/changes/*` 交叉核对。
- 本改动为纯文档，无代码/行为变更；`go test ./...` 与前端门禁不受影响（未改代码）。

## Risks / Notes

- 文档是 M102「证据汇编」而非新证据：所有演练数字均来自 `.artifacts/` 与 change-record，组织侧复验需在授权环境重新生成。
- M89/M90 与真实组织 kind/Helm 生命周期仍为 Deferred；未完成前版本保持 RC，不宣称 GA（五份文档均显式声明该边界）。

# ci-lint-sa4005-fix：修复 CI Backend job golangci-lint SA4005 失败

- Date: 2026-08-14
- Status: Complete
- Scope: backend 测试桩无效赋值（staticcheck SA4005）修复

## Context

- CI run 31762918129（push 4c32e0d）Backend job 失败：`aiexplain_handler_test.go:51:2: SA4005: ineffective assignment to field aiexplainRepoStub.saved (staticcheck)`
- 根因：`aiexplainRepoStub.Save` 为值接收者，`s.saved = *item` 只写入本地副本，外部无法观测；该字段从未被读取，属于无效赋值。

## What Changed

### backend
- `backend/internal/httpserver/aiexplain_handler_test.go`：
  - 删除 `aiexplainRepoStub.saved` 字段（从未被读取）
  - 删除 `Save` 方法内无效赋值 `s.saved = *item`，接口实现与错误返回不变

## Verification

- `golangci-lint run --config ../.golangci.yml ./...`（v2.12.2）：0 issues
- `go test ./... -count=1`：全量 ok
- `gofmt -l .`：无输出

## Risks / Notes

- 纯测试桩清理，不影响生产代码路径
- 若未来需要断言已保存内容，应改用指针接收者或添加观测字段

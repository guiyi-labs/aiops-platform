# M112-4 解释覆盖率大盘：AI 解释可用率 / 引用率 / 降级率只读展示

- Date: 2026-08-14
- Status: Complete
- Scope: M112 第四个切片——基于 aiexplain quality feedback 基线的解释覆盖率只读大盘。

## Context

- 上位路线：`docs/development-roadmap-post-m110.md` Track C 第 143–144 行：基于 `aiexplain` quality feedback 基线，展示解释可用率/引用率/降级率（只读，纯展示）。
- 数据基线：`internal/aiexplain` 的 `Quality()`（quality feedback 汇总，含 by_model）与此前 M55 explanation 持久化（ai_explanations 表）。大盘为纯只读聚合：直接派生自 ai_explanations + ai_explanation_feedback，不新增写路径、不触发 AI 调用、不新增数据库表。

## What Changed

### 后端
- `backend/internal/aiexplain/model.go`：新增 `CoverageSummary`——`total_explanations`、`explained_diagnoses`（解释可用率基线：有 ≥1 条解释的 distinct diagnosis_id）、`with_citations`、`citation_rate`、`deterministic_count`（provider='nop' 的确定性降级条数）、`deterministic_rate`、`quality`（嵌入 QualitySummary 基线）、`window_note`。
- `backend/internal/aiexplain/repository.go`：Repository 接口新增 `Coverage(context) (CoverageSummary, error)`；GormRepository 实现——单条 SQL 从 ai_explanations 聚合计数（citations 非空数组判引用、provider='nop' 判降级），再复用 Quality() 取反馈基线；全量窗口（all-time），无时间窗口参数。
- `backend/internal/aiexplain/service.go`：Service 新增 `Coverage` 透传方法（只读，无副作用）。
- `backend/internal/httpserver/aiexplain.go`：`aiExplanationHandler.coverage`——GET 只读聚合；`AuditAction ai_explanation.coverage.read` / `AuditResource AIExplanationCoverage`。
- `backend/internal/httpserver/router.go`：`Options.AIExplanation` 非 nil 时注册 `GET /api/v1/ai/coverage`。
- 测试：
  - `internal/httpserver/router_harness_test.go`：路由契约测试自动覆盖 coverage 路由。
  - `internal/httpserver/aiexplain_handler_test.go`：`TestAIExplanationCoverage`（200、字段完整性、quality 基线内嵌）；repo stub 补 Coverage。
  - `internal/aiexplain/service_test.go`：`TestServiceCoverage`（服务层透传 + 基线完整性）；repo stub 补 Coverage。
  - `internal/aiexplain/repository.go`：无独立单元测试（SQL 聚合依赖真实 DB，由 cmd/server 集成路线覆盖）。

### OpenAPI / 权限矩阵 / 前端
- `docs/api/openapi.yaml`：新增 `GET /api/v1/ai/coverage`（operationId `aiCoverage`）与 `AICoverage` schema（total/citation_rate/deterministic_rate/quality 基线）。
- `docs/security/permission-matrix.md`：重生成，登记 `ai_explanation.coverage.read`（GET `/api/v1/ai/coverage`）。
- `frontend/src/api/openapi.d.ts`：typegen 重生成。
- `frontend/src/types/diagnosis.ts`：新增 `AICoverage`。
- `frontend/src/api/diagnosis.ts` + `diagnosis.test.ts`：新增 `getAICoverage(token)` 与测试（GET /api/v1/ai/coverage）。
- `frontend/src/views/AICoverageView.vue`（新增）：只读大盘——可用率/引用率/降级率/好评率四卡片、引用与降级进度条、质量反馈基线网格（有帮助/部分/无帮助/贡献者）、按模型明细表；无写操作。
- `frontend/src/router/index.ts` + `components/ConsoleLayout.vue`：路由 `/aiops/ai-coverage` + 侧栏「解释覆盖率」入口。

## Verification

- `go test ./...`：68 包全绿。
- `pnpm typecheck` / `pnpm lint` / `pnpm test -- --run`（152 通过）/ `pnpm build`：全绿。
- OpenAPI 路由匹配 + 权限矩阵一致性：通过。
- 只读性：Coverage 路径零写操作、零 AI 调用；页面无写按钮。
- 无数据库迁移（全部派生字段，无新表/列）。

## Risks / Notes

- `deterministic_count` 目前统计 provider='nop' 的解释（测试/降级路径）；生产 AI 故障路径（M55 Generate 失败）不落 ai_explanations，因此大盘降级率主要反映确定性 provider 走查，不涵盖 AI 故障拒答。若需把 AI 故障计入降级率，后续可扩展 ai_explanations provider 字段语义（migration 000050 候选）。
- 全量窗口聚合（无时间过滤）；若需要近 7/30 天区间，后续可给 Coverage 加 window 参数（前端暂不暴露，按路线只读纯展示交付）。
- M110 RC-6 发布仍待用户授权；M89/M90 授权 Track 保持 Deferred，不阻塞本切片。
- 至此 M112 四个切片（1 驾驶舱 / 2 会话调查 / 3 AI 摘要 / 4 覆盖率大盘）全部完成并各自独立提交。
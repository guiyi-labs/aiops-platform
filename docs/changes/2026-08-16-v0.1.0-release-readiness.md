# v0.1.0 发布准备（release notes + 安装路径修正）

- Date: 2026-08-16
- Status: Complete
- Scope: 用户拍板「CLI + README 完成后直接发 v0.1.0 正式版」；本 commit 备好可发布状态
- DependsOn: README（0fcaacd）、CLI（96c7856）

## What

### 1. README 安装路径修正（硬事实驱动）

实测确认：仓库是无根 go.mod 的 monorepo（Go 模块在 `backend/`，
`module k8s-aiops.local/backend`），所以
`go install github.com/guiyi-labs/aiops-platform/cmd/aiops@latest` **不可用**
（`@latest` 解析到历史根模块 v0.3.0-rc.4，不含 cmd/aiops；根模块无 go.mod）。

README 两处 CLI 安装段改为真实可用路径：
- 首选 **GitHub Release 二进制**（v0.1.0 发布时挂 asset + SHA256）；
- 源码安装 `git clone → cd backend → go install ./cmd/aiops`（实测通过）。

### 2. CHANGELOG 增加 `## [0.1.0] - 2026-08-16` 正式版段落

英文 Highlights（CLI / case memory RAG / 可校验 AI 解释 / ControlledOperation
operator / Web console）+ Quality（覆盖率/lint/gosec/trivy/race/e2e）+ Notes
（monorepo 安装说明）。放于 `## [Unreleased]` 之前（Keep a Changelog 惯例）。
`Unreleased` 区段保留继续积累。

### 3. 新增 `docs/releases/v0.1.0.md` 发布说明

- 版本定位（首个公开版）、提供内容、CLI 安装（二进制 + 源码）、快速上手。
- 兼容范围（K8s 1.34–1.36 / PG17 / Compose/Helm/Kustomize / CLI 平台矩阵）。
- 边界如实：生产 OIDC/MFA 与组织级 HA/WAL-PITR 不宣称；`cases --server`
  知识库端点未随 v0.1.0 面世（CLI 降级内置目录，端点上线自动启用）。
- 发布清单（命令中枢协调）：HEAD 确认 → 门禁全绿 → `v0.1.0` tag **需用户
  显式授权** → 多平台二进制 + SHA256 资产 → README asset 链接 → 同日渠道。

## Verification

- `cd backend && go install ./cmd/aiops` → `aiops --version` 输出 `aiops 0.1.0` ✅
- `go install github.com/guiyi-labs/aiops-platform/cmd/aiops@latest` → 报错，
  与新增的「Notes」说明一致（不再误导网友）✅
- `go test ./...` 未受文档改动影响（纯 docs/README 改动）。

## Artifacts

- `README.md`（CLI 安装段 ×2 修正）
- `CHANGELOG.md`（[0.1.0] 段落）
- `docs/releases/v0.1.0.md`（新增发布说明）